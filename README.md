# vlm-embed

Multimodal PDF embedding and semantic search. Renders each PDF page as an image, embeds it via OpenRouter (Gemini Embedding 2), stores vectors in a local ChromaDB, and provides a Gradio UI for visual semantic search.
